# 🤖 Telegram-бот для продажи ключей

Автоматический бот для продажи цифровых ключей с ручной проверкой оплаты.

## 📋 Возможности

### Для пользователей:
- 🔑 Покупка ключей
- 💳 Оплата через СБП/карта РФ
- 📦 Просмотр истории покупок
- 🆘 Поддержка

### Для администраторов:
- ✅ Подтверждение/отклонение оплат
- 📊 Статистика продаж
- 🔑 Управление ключами
- 📝 Логи всех операций

## 🚀 Установка

### 1. Требования
- Python 3.9+
- VPS или локальный сервер

### 2. Создание бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и скопируйте токен

### 3. Узнайте свой Telegram ID

Напишите боту [@userinfobot](https://t.me/userinfobot) - он пришлёт ваш ID

### 4. Установка на сервере

```bash
# Клонируйте или загрузите файлы проекта
cd telegram-key-bot

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt
```

### 5. Настройка

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
nano .env
```
--------------------------------------------
Заполните:

# Токен бота от @BotFather
BOT_TOKEN=your_telegram_bot_api

# ID администраторов (через запятую)
# Узнать свой ID можно у бота @userinfobot
ADMIN_IDS=123456789,987654321

# База данных (опционально)
DATABASE_PATH=bot_database.db

# Настройки оплаты
PAYMENT_CARD=2200700712345678
PAYMENT_RECIPIENT=Иван И.

# Цена ключа (в рублях)
KEY_PRICE=500

# Контакт поддержки
SUPPORT_USERNAME=@your_support
------------------------------------

### 6. Первый запуск

```bash
python bot.py
```
или 
```bash
python bot-telepot.py
```
(bot-telepot написан для более старых версий python. Python version<3)

Если всё работает, вы увидите: `Бот запущен`

## 📱 Использование

### Для пользователей

1. Найдите бота в Telegram
2. Нажмите `/start`
3. Выберите "🔑 Купить ключ"
4. Получите реквизиты для оплаты
5. Оплатите с указанием комментария
6. Нажмите "Я оплатил"
7. Ждите подтверждения (5-15 минут)
8. Получите ключ автоматически

### Для администраторов

#### Базовые команды:
- `/start` - главное меню
- `/admin` - админ-панель

#### Управление ключами:
```bash
# Добавить один ключ
/addkey ABCD-1234-EFGH-5678

# Сгенерировать 10 ключей
/addkeys 10

# Просмотреть все ключи
/listkeys
```

#### Проверка оплат:
1. При новой оплате придёт уведомление
2. Проверьте платёж в банке
3. Нажмите "✅ Подтвердить" или "❌ Отклонить"
4. Ключ автоматически отправится пользователю

## 🗂 Структура файлов

```
telegram-key-bot/
├── bot.py              # Главный файл бота
├── database.py         # Работа с базой данных
├── key_generator.py    # Генератор ключей
├── requirements.txt    # Зависимости
├── .env               # Конфигурация (создайте сами)
├── .env.example       # Пример конфигурации
└── bot_database.db    # БД SQLite (создаётся автоматически)
```

## 🔧 Настройка автозапуска (Linux)

### Вариант 1: systemd

Создайте файл `/etc/systemd/system/telegram-bot.service`:

```ini
[Unit]
Description=Telegram Key Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/telegram-key-bot
Environment="PATH=/path/to/telegram-key-bot/venv/bin"
ExecStart=/path/to/telegram-key-bot/venv/bin/python bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
sudo systemctl status telegram-bot
```

### Вариант 2: screen

```bash
screen -S telegram_bot
cd /path/to/telegram-key-bot
source venv/bin/activate
python bot.py

# Нажмите Ctrl+A, затем D для выхода
# Вернуться: screen -r telegram_bot
```

## 💡 Советы

### Безопасность:
- ✅ Никогда не публикуйте `.env` файл
- ✅ Держите токен бота в секрете
- ✅ Регулярно делайте бэкапы базы данных

### Бэкап базы данных:
```bash
# Копировать БД
cp bot_database.db bot_database_backup_$(date +%Y%m%d).db

# Автоматический бэкап через cron (ежедневно в 3:00)
0 3 * * * cp /path/to/bot_database.db /path/to/backups/db_$(date +\%Y\%m\%d).db
```

### Добавление товаров:
В будущем можно расширить бот для продажи нескольких типов товаров. Сейчас он настроен на один тип ключа.

## 🐛 Частые проблемы

### Бот не отвечает
- Проверьте, запущен ли `bot.py` |`bot-telepot.py`
- Проверьте правильность токена в `.env`
- Убедитесь, что нет других ботов с тем же токеном

### Ключи не выдаются
- Проверьте наличие ключей: `/listkeys`
- Добавьте ключи: `/addkeys 10`

### Не приходят уведомления админу
- Проверьте `ADMIN_IDS` в `.env`
- Убедитесь, что ID указан правильно

## 📊 База данных

### Таблицы:
- `users` - пользователи
- `keys` - ключи
- `orders` - заказы
- `purchases` - история покупок
- `logs` - логи действий

### Просмотр БД:
```bash
sqlite3 bot_database.db
.tables
SELECT * FROM users;
.quit
```

## 🔄 Обновление

```bash
git pull  # если используете git
source venv/bin/activate
pip install -r requirements.txt --upgrade
sudo systemctl restart telegram-bot  # если используете systemd
```

## 📞 Поддержка

Если возникли вопросы:
1. Проверьте логи: `journalctl -u telegram-bot -f`
2. Проверьте базу данных
3. Убедитесь, что все зависимости установлены

## ⚡️ Масштабирование

Для большого количества пользователей:
- Замените SQLite на PostgreSQL
- Используйте Redis для кэширования
- Настройте nginx reverse proxy
- Добавьте мониторинг (Prometheus + Grafana)

## 📝 Лицензия

MIT License - используйте как хотите!

---

**Сделано с ❤️ для автоматизации продаж**